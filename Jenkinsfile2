pipeline {
  
  agent any
  
  stages {
    stage('Build Stage') {
      steps {
        checkout scm
        echo "building"
      }
    }
    stage('Call Job') {
      steps {
        script {
          jobStatus = build job: "job1", wait: true
          echo jobStatus.getResult()
        }
      }
    }
        

  } // stages
} // pipeline
